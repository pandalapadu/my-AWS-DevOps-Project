- name: Demo on Play level Variable discussions ##top we can called as play level variables 
  hosts: localhost
  become: yes
  connection: local # it will not ask any username or passsword 
  vars:
    COURSE: "Ansible"
    TRAINER: "venkat"
    DURATION: "150HRs"
  tasks:
    - debug:
        msg: "Hi I am learing {{ COURSE }}, from {{ TRAINER }}, class hours  is {{ DURATION }} "