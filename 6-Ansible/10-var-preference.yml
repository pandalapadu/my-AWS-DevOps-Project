- name: Checking all passing input variable preferrences 
  hosts: frontend
  vars:
    full_name: "venkat from playlevel"
  vars_files:
  - course.yml
  # vars_prompt:
  # - name: fullname
  #   #prompt: "please enter {{ full_name }}"
  #   private: false
  tasks:
    - name: print vars preference
      # vars:
      # #  full_name: "venkat from tasklevel"
      debug:
        msg: "Hello {{ full_name }}"

#1st Preferences is "CMD arguments" 
#2nd Preferences is "task level"
#3rd Preferences is "vars_files"
#4th Preferences is