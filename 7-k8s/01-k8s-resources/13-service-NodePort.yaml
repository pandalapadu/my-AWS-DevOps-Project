apiVersion: v1
kind: Service
metadata:
  name: nginx-nodeport
  labels:
    project: roboshop
    environment: dev
spec:
  type: NodePort
  selector:
    project: roboshop
    tier: frontend
    component: frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30007