@Library('roboshop-shared-library') _
// Pre-populated map (keys default to strings automatically)
def configMap = [
    component : 'catalogue',
    project   : 'roboshop',
    port      : 8080,
    isActive  : true
]
if (env.BRANCH_NAME.string1.equalsIgnoreCase('main')) {
    echo "we will build it later"
}
else {
    nodejsEKSpipeline(configMap)
}
