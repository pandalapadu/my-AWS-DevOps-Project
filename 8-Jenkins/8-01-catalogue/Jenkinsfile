@Library('roboshop-shared-library') _
// Pre-populated map (keys default to strings automatically)
def configMap = [
    component : 'catalogue',
    project   : 'roboshop',
    port      : 8080,
    isActive  : true
]
testpipeline(configMap)